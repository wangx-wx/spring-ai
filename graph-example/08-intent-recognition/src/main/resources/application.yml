server:
  port: 10028
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: intent-recognition
  ai:
    dashscope:
      api-key: ${DASH_SCOPE_API_KEY}
      intent-knowledge: uat-意图识别
      qa-knowledge: 客服知识库
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/db
    username: pgsql
    password: 123456